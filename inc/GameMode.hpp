/**
 * @file GameMode.hpp
 * @brief Declares the GameMode enum.
 *
 * GameMode is used to identify which type of game session
 * to create in the factory.
 */

 #pragma once

 /**
  * @brief Represents the various modes the game can operate in.
  */
 enum class GameMode
 {
     ConsoleSinglePlayer = 0
 };
 